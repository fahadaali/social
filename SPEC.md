# مواصفات مشروع: بوت النشر الذكي (Telegram → Claude → SocialAPI)

> **تعليمات لـ Claude Code:** هذا الملف هو المرجع الوحيد للمشروع. نفّذه على مراحل كما في القسم 12، والتزم بقسم "المحظورات". قبل كتابة أي استدعاء لـ SocialAPI اقرأ التوثيق الرسمي من `https://docs.social-api.ai/llms.txt` وملف `https://docs.social-api.ai/api-reference/openapi.json`، ولا تخمّن أسماء المسارات أو الحقول. إذا تعارض شيء هنا مع التوثيق، اتبع التوثيق وسجّل الملاحظة في `NOTES.md`.

---

## 1. الهدف

مساعد شخصي على تيليجرام يحافظ على وتيرة نشر ثابتة في X ولينكدن (وسناب شات يدوياً)، عبر:

- حفظ الأفكار الخام التي يرسلها المستخدم في بنك محتوى.
- اقتراح موضوعات أسبوعية من بنك الأفكار ومحاور المحتوى.
- صياغة المنشور بصيغ متعددة بأسلوب المستخدم.
- النشر أو الجدولة **بعد موافقة صريحة فقط**.
- التذكير عند انقطاع النشر، وتقرير أداء أسبوعي.

المستخدم الوحيد للبوت هو المالك. لا يوجد أي مستخدم آخر.

## 2. البنية التقنية

| المكوّن | الاختيار |
|---|---|
| الاستضافة | Cloudflare Workers (الخطة المجانية) |
| اللغة | TypeScript |
| قاعدة البيانات | Cloudflare D1 |
| الجدولة | Cron Triggers (3 فقط، ضمن حد الخطة المجانية) |
| الواجهة | Telegram Bot API عبر webhook، باستدعاءات `fetch` مباشرة بدون أطر عمل ثقيلة |
| الذكاء الاصطناعي | Anthropic Messages API |
| النشر والتحليلات | SocialAPI.ai (`https://api.social-api.ai/v1`) |
| الكود والنشر | مستودع GitHub خاص + Cloudflare Workers Builds (ربط Git من لوحة Cloudflare) |

### قيود الخطة المجانية التي يجب احترامها في التصميم

- **وقت المعالجة:** حد المعالجة 10ms لكل طلب، ووقت انتظار الشبكة لا يُحتسب منه. لذلك تجنّب أي معالجة ثقيلة داخل الـ Worker (لا معالجة صور، ولا تحليل ملفات كبيرة).
- **مهلة تيليجرام:** يجب الرد على webhook تيليجرام فوراً بـ 200، ثم إكمال العمل عبر `ctx.waitUntil()`. أرسل للمستخدم رسالة "جاري الصياغة…" أولاً، ثم عدّلها بالنتيجة (`editMessageText`).
- **مدة المهام في الخلفية:** `waitUntil` محدود زمنياً. اجعل استدعاءات Claude قصيرة ومحددة الطول (`max_tokens` مضبوط). إذا تكررت انقطاعات بسبب المهلة، سجّل ذلك في `NOTES.md` واقترح الانتقال إلى Cloudflare Queues، ولا تنتقل إليها بدون موافقة.
- **Cron:** يعمل بتوقيت UTC فقط. توقيت الرياض = UTC+3 بدون توقيت صيفي.
- **لا إعادة محاولة تلقائية:** Cron لا يعيد المحاولة عند الفشل. أي فشل في مهمة مجدولة يجب أن يُرسل تنبيهاً للمستخدم على تيليجرام.

## 3. المتغيرات والأسرار

### أسرار (`wrangler secret put`)

| الاسم | الوصف |
|---|---|
| `TELEGRAM_BOT_TOKEN` | من BotFather |
| `TELEGRAM_WEBHOOK_SECRET` | نص عشوائي طويل، يُمرَّر في `setWebhook` كـ `secret_token` |
| `ANTHROPIC_API_KEY` | مفتاح Anthropic |
| `SOCIALAPI_KEY` | مفتاح SocialAPI (يُفضّل مفتاح بصلاحيات محدودة: المنشورات والوسائط والتحليلات فقط) |

### متغيرات (`[vars]` في `wrangler.toml`)

| الاسم | الوصف | القيمة الافتراضية |
|---|---|---|
| `ALLOWED_TELEGRAM_USER_ID` | معرّف تيليجرام الرقمي للمالك | يُملأ لاحقاً |
| `SOCIALAPI_X_ACCOUNT_ID` | معرّف حساب X في SocialAPI | يُملأ لاحقاً |
| `SOCIALAPI_LINKEDIN_ACCOUNT_ID` | معرّف حساب لينكدن في SocialAPI | يُملأ لاحقاً |
| `CLAUDE_MODEL` | نموذج الصياغة | `claude-sonnet-5` |
| `CLAUDE_MODEL_FAST` | نموذج التصنيف والمهام الخفيفة | `claude-haiku-4-5-20251001` |
| `REMINDER_AFTER_DAYS` | عدد الأيام بدون نشر قبل التذكير | `4` |
| `MONTHLY_POST_LIMIT` | حد المنشورات في خطة SocialAPI | `10` |
| `DRY_RUN` | عند `true` تُحفظ المنشورات كمسودات في SocialAPI بدون نشر (المسودات لا تستهلك رصيداً) | `true` |

## 4. الأمان

1. **التحقق من المصدر:** كل طلب webhook يجب أن يحمل الترويسة `X-Telegram-Bot-Api-Secret-Token` مطابقة لـ `TELEGRAM_WEBHOOK_SECRET`، وإلا يُرفض بـ 401.
2. **التحقق من المستخدم:** أي تحديث من مستخدم غير `ALLOWED_TELEGRAM_USER_ID` يُتجاهل بصمت، بدون أي رد.
3. **منع التكرار:** يُخزّن `update_id` في D1، ويُتجاهل أي تحديث مكرر.
4. **حماية التوكن:** لا يُرسل رابط ملف تيليجرام إلى أي طرف ثالث، لأنه يحتوي توكن البوت. الصور تُنزّل كبايتات ثم تُرفع إلى SocialAPI (انظر القسم 8).
5. **السجلات:** لا تُسجَّل الأسرار أو نصوص المنشورات الكاملة في السجلات.

## 5. قاعدة البيانات (D1)

ملف الترحيل: `migrations/0001_init.sql`

```sql
CREATE TABLE ideas (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  text TEXT NOT NULL,
  pillar TEXT,                 -- محور المحتوى المصنّف آلياً
  source TEXT NOT NULL DEFAULT 'text', -- text | voice | photo
  status TEXT NOT NULL DEFAULT 'new',  -- new | drafted | published | archived
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE drafts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  idea_id INTEGER REFERENCES ideas(id),
  x_segments TEXT NOT NULL,    -- JSON array: التغريدة الأولى ثم بقية الثريد
  linkedin_text TEXT NOT NULL,
  snap_script TEXT,            -- سكربت سناب (للنشر اليدوي)
  needs_visual INTEGER NOT NULL DEFAULT 0,
  visual_brief TEXT,           -- وصف مقترح للتصميم في Claude Design
  media_id TEXT,               -- معرّف الوسائط في SocialAPI بعد الرفع
  platforms TEXT NOT NULL DEFAULT '["x","linkedin"]',
  status TEXT NOT NULL DEFAULT 'pending', -- pending | scheduled | publishing | published | partial | failed | rejected
  socialapi_post_id TEXT,
  scheduled_at TEXT,
  published_at TEXT,
  telegram_message_id INTEGER, -- رسالة المعاينة لتحديثها
  revision INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE state (
  key TEXT PRIMARY KEY,        -- مثل: awaiting_edit, awaiting_image, reminders_paused, last_published_at
  value TEXT
);

CREATE TABLE processed_updates (
  update_id INTEGER PRIMARY KEY,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

احذف سجلات `processed_updates` الأقدم من 7 أيام ضمن مهمة Cron اليومية.

## 6. هيكل المستودع

```
/
├── SPEC.md                  ← هذا الملف
├── NOTES.md                 ← ملاحظات التنفيذ والانحرافات عن المواصفات
├── wrangler.toml
├── package.json
├── tsconfig.json
├── migrations/0001_init.sql
├── config/
│   ├── voice.md             ← دليل أسلوب المالك ونماذج من كتاباته (يملؤه المالك)
│   └── pillars.md           ← محاور المحتوى (يملؤه المالك)
└── src/
    ├── index.ts             ← fetch() للـ webhook + scheduled() للـ Cron
    ├── telegram.ts          ← إرسال، تعديل، أزرار، تنزيل ملفات
    ├── claude.ts            ← استدعاء Messages API وتحليل JSON
    ├── socialapi.ts         ← المنشورات، الوسائط، المقاييس، الاستهلاك
    ├── db.ts
    ├── time.ts              ← تحويل توقيت الرياض ↔ UTC
    ├── handlers/
    │   ├── messages.ts
    │   ├── callbacks.ts
    │   └── cron.ts
    └── prompts/
        ├── classify.ts
        ├── draft.ts
        ├── revise.ts
        ├── plan.ts
        └── report.ts
```

يُقرأ `config/voice.md` و`config/pillars.md` عند البناء كنصوص مضمّنة (import as text) وليس من الشبكة.

## 7. تجربة المستخدم على تيليجرام

كل نصوص البوت بالعربية، بلغة مهذبة ومختصرة.

### الأوامر

| الأمر | الوظيفة |
|---|---|
| `/start` | رسالة ترحيب مختصرة وقائمة الأوامر |
| أي نص عادي | يُحفظ فكرةً جديدة، ويُصنّف محورها آلياً، ويرد البوت: "حُفظت الفكرة #12 (محور: الحوكمة)" مع زر **صُغها الآن** |
| `/ideas` | آخر 10 أفكار بحالة `new`، لكل فكرة زر **صُغها** |
| `/plan` | يقترح 3 موضوعات الآن (نفس منطق الخطة الأسبوعية) |
| `/queue` | المسودات المعلّقة والمجدولة |
| `/stats` | تقرير الأداء الآن |
| `/usage` | رصيد المنشورات المتبقي هذا الشهر في SocialAPI |
| `/pause` و`/resume` | إيقاف واستئناف التذكيرات |
| `/help` | المساعدة |

### رسالة معاينة المسودة

ترسل الرسالة الواحدة المحتوى التالي:

```
📝 مسودة #7 — المحور: الحوكمة (نسخة 1)

【X — ثريد من 4 تغريدات】
1/ …
2/ …

【LinkedIn】
…

【سناب — للنشر اليدوي】
…

🖼 يُقترح تصميم: بطاقة تلخّص الخطوات الثلاث (صمّمها في Claude Design وأرسلها هنا)
```

إذا تجاوزت الرسالة حد تيليجرام (4096 حرفاً)، تُقسّم إلى رسالة معاينة X ورسالة معاينة لينكدن، وتبقى الأزرار في الرسالة الأخيرة.

### أزرار المسودة (Inline Keyboard)

| الزر | الإجراء |
|---|---|
| ✅ انشر الآن | ينشر على المنصات المفعّلة (انظر القسم 8) |
| 🕒 جدول | يعرض خيارات: اليوم 8م، غداً 8ص، غداً 8م، بعد غد 8ص (بتوقيت الرياض) |
| ✏️ عدّل | يضبط حالة `awaiting_edit`، ويطلب من المستخدم كتابة ملاحظاته، ثم يعيد الصياغة وفقها |
| 🔁 صياغة جديدة | يعيد الصياغة من الصفر بزاوية مختلفة |
| 🖼 أرفق صورة | يضبط حالة `awaiting_image` ويطلب إرسال الصورة |
| X ✓ / LinkedIn ✓ | تبديل تفعيل كل منصة لهذه المسودة |
| 🗑 تجاهل | يضع المسودة `rejected` والفكرة `archived` |

**قاعدة غير قابلة للتجاوز:** لا يُنشر ولا يُجدول أي شيء إلا بضغطة زر من المالك. لا يوجد أي مسار آلي للنشر.

بعد الضغط على **انشر الآن** أو **جدول**، يظهر تأكيد أخير: "تأكيد النشر على X ولينكدن؟ (المتبقي من رصيد الشهر: 6)" بزرين: **تأكيد** و**إلغاء**.

## 8. التكامل مع SocialAPI

### معلومات مؤكدة من التوثيق

- **إنشاء المنشور:** `POST /v1/posts` مع `targets` (مصفوفة `account_id`)، ويدعم `text` و`title` على مستوى المنشور، وتجاوزات لكل هدف (`targets[].text`، `targets[].title`).
- **أوضاع الإنشاء:** `publish_now: true` للنشر الفوري، و`scheduled_at` (RFC3339) للجدولة. بدون أيهما تُحفظ المسودة، وهي لا تستهلك رصيداً.
- **الرصيد:** المنشور الواحد يستهلك رصيداً واحداً عند نشره أو جدولته.
- **الحالة:** الحالة النهائية (`published` / `partial` / `failed`) تُعرف بالاستعلام عبر `GET /v1/posts/{pid}`، لأن الـ webhooks غير متاحة في الخطة المجانية.
- **الثريد:** `segments` تُنشر كثريد أصلي على X.
- **التحقق المسبق:** يوجد مسار تحقق تجريبي (Validate post content) يعيد الأخطاء والتحذيرات بدون نشر.
- **المقاييس:** `GET /v1/posts/{pid}/metrics`، ومسار استهلاك الفترة الحالية (Get resource usage).
- **الوسائط:** يوجد مسار رفع من الخادم (Upload media server-side). راجع التوثيق لاسم الحقول الدقيق.
- **حساب X:** يتطلب إعداد BYOK (تطبيق مطوّر خاص بالمالك). هذا إعداد يدوي يقوم به المالك، وليس ضمن الكود.
- **لينكدن:** المدعوم للنشر هو الحساب الشخصي فقط. صفحات الشركات ما زالت قادمة قريباً.

### منطق النشر

1. **التحقق أولاً:** قبل أي نشر، استدعِ مسار التحقق. إذا ظهرت أخطاء، اعرضها للمستخدم ولا تنشر.
2. **منشور واحد لكل مسودة لتوفير الرصيد:** هدفان في منشور واحد:
   - هدف X: `text` = التغريدة الأولى، و`segments` = بقية الثريد.
   - هدف LinkedIn: `text` = نص لينكدن عبر تجاوز الهدف.
3. **نقطة غير محسومة يجب اختبارها:** `segments` معرّفة على مستوى المنشور، ولا يوضّح التوثيق هل يتجاهلها هدف لينكدن أم لا. اختبر ذلك بمسودة (مجانية) وبمسار التحقق:
   - إذا تجاهلها لينكدن: استخدم منشوراً واحداً.
   - إذا لم يتجاهلها: استخدم منشورين منفصلين، وحدّث رسالة التأكيد لتوضح أن النشر سيستهلك رصيدين. سجّل النتيجة في `NOTES.md`.
4. **وضع التجربة:** عند `DRY_RUN=true` يُنشأ المنشور كمسودة فقط، ويرد البوت: "🧪 وضع التجربة: حُفظت كمسودة في SocialAPI ولم تُنشر".
5. **متابعة الحالة:** بعد `publish_now` استعلم عن الحالة حتى 3 مرات ضمن `waitUntil` بفاصل 5 ثوانٍ. إذا بقيت `publishing`، تتولاها مهمة Cron اليومية. عند الوصول لحالة نهائية، حدّث D1 وأرسل للمستخدم الروابط (`permalink`) أو الخطأ.
6. **حارس الرصيد:** قبل التأكيد، اجلب الاستهلاك. إذا كان الرصيد المتبقي 0، امنع النشر واعرض السبب. إذا كان 2 أو أقل، أضف تنبيهاً في رسالة التأكيد.
7. **آخر نشر:** بعد كل نشر ناجح، حدّث `state.last_published_at`.

### الصور

1. المالك يصمم في Claude Design، ثم يرسل الصورة للبوت وهو في حالة `awaiting_image`.
2. البوت يأخذ أكبر نسخة من `photo`، ويستدعي `getFile`، وينزّل البايتات داخل الـ Worker.
3. البوت يرفع البايتات إلى SocialAPI عبر مسار الرفع من الخادم، ويحفظ `media_id` في المسودة.
4. تُرفق الصورة بهدفي X ولينكدن كليهما، ما لم يطلب المالك غير ذلك.
5. إذا أُرسلت صورة بدون حالة `awaiting_image`، تُعامل كفكرة جديدة مصدرها `photo`، والتعليق المرفق بها هو نصها.

## 9. المهام المجدولة (3 Cron)

| الموعد (UTC) | بتوقيت الرياض | المهمة |
|---|---|---|
| `0 6 * * *` | يومياً 9:00 ص | (أ) متابعة المنشورات العالقة في حالة `publishing`. (ب) إذا مرّ `REMINDER_AFTER_DAYS` أيام أو أكثر على آخر نشر، ولا يوجد منشور مجدول، والتذكيرات غير موقوفة: أرسل تذكيراً بزرين **اقترح موضوعاً** و**أفكاري**. (ج) تنظيف `processed_updates` |
| `0 5 * * 0` | الأحد 8:00 ص | الخطة الأسبوعية: 3 موضوعات مقترحة، لكل منها زر **صُغها** |
| `0 14 * * 4` | الخميس 5:00 م | تقرير الأداء الأسبوعي |

عند أي استثناء في مهمة Cron: أرسل للمالك "⚠️ فشلت مهمة [الاسم]: [ملخص الخطأ]".

## 10. تعليمات Claude (Prompts)

جميع الاستدعاءات تطلب مخرجات **JSON فقط** وفق مخطط محدد، ويُتحقق منها قبل الاستخدام. عند فشل التحليل، تُعاد المحاولة مرة واحدة، ثم يُبلَّغ المستخدم.

### القواعد المشتركة (تُضمَّن في كل System Prompt)

- الكاتب محامٍ سعودي ومستشار في الحوكمة والتخطيط الاستراتيجي، والجمهور مهنيون وقياديون في القطاعين الخاص وغير الربحي.
- العربية الفصحى المعاصرة، رصينة وواضحة، بلا حشو ولا مبالغة ولا إيموجي كثيرة.
- **يُمنع اختلاق** أي رقم مادة نظامية أو اسم نظام أو إحصائية أو مصدر غير موجود في نص الفكرة. إذا احتاج المنشور مرجعاً غير متوفر، يُكتب بصيغة عامة، وتُضاف ملاحظة في حقل `notes` تطلب من المالك التحقق.
- لا يتضمن المنشور استشارة في قضية بعينها، ولا وعوداً بنتائج، ولا أي معلومة عن عملاء.
- يُلتزم بما في `config/voice.md` من أسلوب ونماذج.

### classify (النموذج السريع)

المدخل نص الفكرة وقائمة المحاور، والمخرج:

```json
{ "pillar": "string", "summary": "string ≤ 15 كلمة" }
```

### draft

المدخل الفكرة والمحور وآخر 5 منشورات منشورة (لتجنب التكرار)، والمخرج:

```json
{
  "x_segments": ["التغريدة الأولى (خطّاف قوي)", "…"],
  "linkedin_text": "string",
  "snap_script": ["إطار 1", "إطار 2", "…"],
  "needs_visual": true,
  "visual_brief": "string أو null",
  "notes": "string أو null"
}
```

**قيود X:**
- 1 إلى 6 تغريدات، كل تغريدة ضمن حد X للأحرف.
- تحقق من الطول برمجياً بعد التوليد. إذا تجاوزت أي تغريدة الحد، أعد الطلب مرة مع ذكر الخطأ.

**قيود LinkedIn:**
- 120 إلى 300 كلمة.
- افتتاحية جاذبة وفقرات قصيرة، وخاتمة بسؤال أو خلاصة.
- ضمن حد لينكدن للأحرف.

**قيود سناب:**
- 3 إلى 5 إطارات، كل إطار سطران كحد أقصى.

### revise

المدخل المسودة الحالية وملاحظات المالك بنصها، والمخرج نفس مخطط `draft`. يُعدَّل فقط ما تطلبه الملاحظات.

### plan

المدخل الأفكار ذات الحالة `new`، والمحاور، وعناوين آخر 10 منشورات، والمخرج:

```json
{ "suggestions": [ { "idea_id": 12, "angle": "string", "why_now": "string" } ] }
```

- 3 اقتراحات موزعة على محاور مختلفة، مع تفضيل الأفكار الأقدم.
- إذا قلّت الأفكار عن 3، يُسمح باقتراح موضوع جديد من المحاور، ويُعاد بـ `idea_id: null`.

### report

المدخل مقاييس منشورات آخر 30 يوماً (إعجابات، تعليقات، مشاركات، حفظ)، والمخرج:

```json
{
  "headline": "string",
  "top_post": "string",
  "insight": "string",
  "next_week_tip": "string"
}
```

- يُعرض التقرير كرسالة قصيرة.
- **لا يُستنتج ما لا تدعمه الأرقام.** إذا كانت البيانات قليلة، يُذكر ذلك صراحة.

## 11. معالجة الأخطاء

| الحالة | الإجراء |
|---|---|
| خطأ 429 من SocialAPI | "بلغتَ حد منشورات الشهر في SocialAPI" بدون إعادة محاولة |
| خطأ 401 أو خطأ صلاحيات لمنصة | "يلزم إعادة ربط حساب [المنصة] من لوحة SocialAPI" |
| خطأ من Anthropic | إعادة محاولة واحدة بعد ثانيتين، ثم رسالة للمستخدم مع زر **أعد المحاولة** |
| أي خطأ غير متوقع | رسالة مختصرة للمستخدم بدون تفاصيل تقنية حساسة، مع سجل مختصر في `console.error` |

## 12. مراحل التنفيذ ومعايير القبول

### المرحلة 1: الأساس

**النطاق:**
- webhook، والأمان، و`/start`، وحفظ الأفكار وتصنيفها.
- الصياغة بزر **صُغها الآن**، وأزرار المسودة كاملة.
- النشر والجدولة عبر SocialAPI مع `DRY_RUN`.
- إرفاق الصور.
- مهمة Cron اليومية.

**القبول:**
1. رسالة من حساب غير المالك لا تلقى أي رد.
2. فكرة نصية → مسودة كاملة خلال أقل من 60 ثانية.
3. في وضع `DRY_RUN=true` تظهر مسودة في لوحة SocialAPI، ولا يُستهلك أي رصيد.
4. اختبار سلوك `segments` مع لينكدن موثّق في `NOTES.md`.
5. التعديل بالملاحظات يعمل ويرفع رقم `revision`.
6. صورة مرسلة تُرفق وتظهر في المسودة على SocialAPI.

### المرحلة 2: الانتظام

**النطاق:**
- `/plan` والخطة الأسبوعية.
- `/queue` و`/usage` و`/pause` و`/resume`.
- الرسائل الصوتية: تفريغ الصوت إلى نص ثم حفظه كفكرة مصدرها `voice`. قبل التنفيذ، ابحث عن خيار تفريغ يدعم العربية ويعمل من Worker (مثل نماذج Whisper في Workers AI)، واعرض الخيار وتكلفته في `NOTES.md` وانتظر الموافقة.

### المرحلة 3: التحليلات

**النطاق:**
- `/stats` والتقرير الأسبوعي.
- استخدام مقاييس المنشورات في تحسين اقتراحات `plan`.

## 13. المحظورات

- لا نشر ولا جدولة بدون ضغطة تأكيد من المالك.
- لا حذف لأي منشور منشور، ولا لأي ملف في المستودع، بدون إذن صريح.
- لا تعديل على `config/voice.md` أو `config/pillars.md` إلا بطلب المالك.
- لا تخمين لمسارات SocialAPI أو حقولها: المرجع هو التوثيق الرسمي.
- لا إضافة خدمات مدفوعة أو اعتماديات جديدة كبيرة بدون موافقة.
- لا تسجيل للأسرار أو لروابط ملفات تيليجرام.

## 14. خطوات الإعداد اليدوية (يقوم بها المالك)

1. **تيليجرام:** أنشئ البوت من `@BotFather` واحفظ التوكن، واحصل على معرّفك الرقمي من `@userinfobot`.
2. **SocialAPI.ai:**
   - أنشئ حساباً وعلامة (Brand) واحدة.
   - اربط لينكدن الشخصي.
   - أعدّ X عبر BYOK: أنشئ تطبيقاً في `developer.x.com` بصلاحيات القراءة والكتابة، وفعّل OAuth 2.0، ثم الصق Client ID وClient Secret في إعدادات SocialAPI، ثم اربط الحساب.
   - أنشئ مفتاح API، وانسخ معرّفي الحسابين.
3. **Anthropic:** أنشئ مفتاح API من Console، وضع حداً شهرياً للإنفاق.
4. **GitHub:** أنشئ مستودعاً خاصاً، وضع فيه هذا الملف، وعبّئ `config/voice.md` (5 إلى 10 نماذج من كتاباتك) و`config/pillars.md` (محاورك).
5. **Cloudflare:** أنشئ حساباً، ثم دع Claude Code ينشئ قاعدة D1 ويضبط الأسرار. بعدها اربط المستودع من Workers & Pages عبر Import from Git.
6. **التفعيل:** بعد النشر، يضبط Claude Code الـ webhook عبر `setWebhook` مع `secret_token`.
7. **التجربة:** جرّب أسبوعاً على `DRY_RUN=true`، ثم حوّلها إلى `false`.
