# ملاحظات التنفيذ

يسجّل هذا الملف مصادر التوثيق، والقرارات، والانحرافات عن `SPEC.md`، وما يحتاج تأكيداً من المالك.

---

## 1. مصدر توثيق SocialAPI

- **الموقع `docs.social-api.ai` محجوب** في بيئة البناء السحابية بسبب سياسة الشبكة، وكذلك `api.social-api.ai` و`api.telegram.org` و`api.cloudflare.com`.
- البديل الرسمي: مستودع التوثيق **[SocialAPI-AI/docs](https://github.com/SocialAPI-AI/docs)**، وهو مصدر الموقع نفسه (Mintlify). اعتمدت على الإيداع `520845d` بتاريخ 2026-09-22. فيه الملف المطلوب في SPEC حرفياً (`api-reference/openapi.json`)، والصفحات التي يُبنى منها `llms.txt`.
- الصفحات المقروءة: `posts/overview` و`posts/linkedin` و`posts/scheduling` و`connectors/twitter` و`connectors/twitter-byok`، و`guides/errors` و`media` و`rate-limits` و`scoped-keys` و`concepts`.
- للمقارنة فقط: حزمتا SocialAPI الرسميتان على npm (`n8n-nodes-socialapi@0.1.6` و`mcp-socialapi@1.2.0`). عند التعارض فالمرجع `openapi.json`. مثال: وصف أداة MCP القديم يقول إن وجود الأهداف يعني النشر، بينما OpenAPI يقول إن المسودة هي الافتراض.

### المسارات والحقول المستخدمة (كلها من `openapi.json`)

| الغرض | المسار | الحقول المستخدمة |
|---|---|---|
| المصادقة | كل الطلبات | `Authorization: Bearer <SOCIALAPI_KEY>`، والأساس `https://api.social-api.ai/v1` |
| الرصيد | `GET /usage` | `posts_used` و`posts_limit` (`-1` = غير محدود) و`period_end` |
| التحقق | `POST /posts/validate` | الطلب: `text`، `targets`، `account_ids`، `scheduled_at`. الرد: `valid`، `errors[]`، `warnings[]` (`platform`، `field`، `message`، `segment_index`) |
| الإنشاء | `POST /posts` | `targets[].{account_id, text, media, platform_data}`، `publish_now`، `scheduled_at`. الردود: 201 و207 (جزئي سريع) و422 (فشل كل الأهداف سريعاً مع كائن المنشور) |
| الحالة | `GET /posts/{pid}` | `status`، و`scheduled_at`، و`targets[].{platform, status, permalink, error}` |
| رفع الوسائط | `POST /media/upload` | `multipart/form-data` بالحقل `file` (حتى 50MB)، ويعيد `media_id` جاهزاً |
| تغيير الموعد | `PATCH /posts/{pid}` | `scheduled_at` فقط. يقبله للحالات `draft` و`scheduled` و`failed`، ويرد 409 إن بدأ النشر |
| إلغاء الجدولة | `DELETE /posts/{pid}` | يلغي المجدول، **لكنه يحذف المنشور المنشور فعلاً من المنصات**. بعد الحذف يرد `GET` بـ 404 |
| إعادة المحاولة | `POST /posts/{pid}/retry` | للحالتين `partial` و`failed`. الرفض: 400 `post.no_retryable_deliveries` و409 `post.state_invalid` و429 |

- **الاستهلاك** (`guides/rate-limits`): يستهلك رصيداً واحداً كلٌّ من الإنشاء بنشر أو جدولة، و`publish`، و`retry`. وما عداها مجاني، ومنه تغيير الموعد والحذف والقراءة.

- **وضع المسودة مؤكَّد من التوثيق:** «بدون `publish_now` ولا `scheduled_at` يُحفظ المنشور مسودة، وهذا هو الافتراض حتى مع وجود أهداف»، والمسودة لا تستهلك رصيداً. لذلك لا يرسل `DRY_RUN` أياً من الحقلين. الاختبارات الطرفية تتحقق من ذلك.
- `media_ids` مهجور حسب OpenAPI، واستخدمت `media: [{source_type: "media_id", source}]`.

---

## 2. قرارات وانحرافات عن SPEC (للاطلاع والموافقة)

### 2.1 الثريد عبر `platform_data.thread` بدل `segments` ✅ (وافق المالك على الإبقاء عليه)
- يوثّق SocialAPI طريقتين: `segments` على مستوى المنشور (OpenAPI)، و`platform_data.thread` على هدف X وحده (صفحة موصل X، وقد رُوجعت مقابل الـ API في تدقيق 2026-06-28).
- اخترت **`platform_data.thread`** لأنه خاص بهدف X، فلا يصل شيء منه إلى هدف لينكدن. هكذا يزول السؤال المفتوح في SPEC §8.3 من الأساس، ويبقى النشر **منشوراً واحداً برصيد واحد**.
- التحقق الفعلي على حسابك ما زال مطلوباً (القسم 4 أدناه). التحويل إلى `segments` تغيير سطر واحد في `buildPostRequest` داخل `src/socialapi.ts`.
- **تغيّرت صيغة العنصر (2026-09-23):** في أول تجربة حقيقية رفض التحقق الثريد برسالة `thread must be an array of {text, media_ids} objects`. التوثيق المنشور ما زال يقول `array of string`، سواء في مستودع التوثيق (الإيداع `520845d`، وهو الأحدث) أو في صفحة موصل X، وموقع التوثيق نفسه محجوب في بيئة البناء. لذلك المرجع هنا رسالة الـ API نفسها: صار كل عنصر `{ "text": "...", "media_ids": [] }`.
  - الخادم الوهمي في الاختبارات الطرفية يرفض الآن الصيغة القديمة بالرسالة نفسها، فلا يعود الخطأ دون أن تكشفه الاختبارات.
  - لا يمكن التأكد من حفظ الثريد بقراءة المنشور بعد إنشائه، لأن `GET /posts/{id}` لا يعيد `platform_data`. الضمان هو خطوة التحقق قبل التأكيد: أخطاؤها توقف النشر، وتنبيهاتها (ومنها الحقول غير المعروفة) تظهر في رسالة التأكيد.

### 2.2 الصور تُرفق بهدف لينكدن فقط ⚠️
- SPEC §8 (الصور 4) يطلب إرفاق الصورة بالهدفين. لكن صفحة موصل X تقول: *«Text-only posts… image and video attachments are ignored»*، وجدول الإمكانات يقول إن X لا يقبل وسائط (0).
- لذلك تُرسل الصورة ضمن هدف لينكدن فقط، وتنبّه المعاينة ورسالة التأكيد إلى ذلك. إن أضافت SocialAPI دعم الوسائط لـ X يكفي تعديل سطر واحد.

### 2.3 جدولة X
- التوثيق متعارض: جدول في `posts/overview` يقول «لا جدولة لـ X»، وصفحة `posts/scheduling` تقول إن كل المنصات تقبل `scheduled_at` (SocialAPI تحتفظ بالمنشور وتنشره في موعده).
- الحل دون تخمين: يُمرَّر `scheduled_at` إلى مسار التحقق قبل التأكيد. إن رفضه X ظهرت الأخطاء ولا يُجدول شيء، ويمكنك حينها إيقاف X للمسودة أو النشر فوراً.

### 2.4 خطأ 429
- SPEC §11 يربط 429 كله برسالة «بلغتَ حد منشورات الشهر». لكن دليل الأخطاء يوثّق عدة رموز للحالة 429:
  - `billing.post_limit`: الحد الشهري، وتظهر له رسالة SPEC نفسها.
  - `publishing.velocity_limit` و`ratelimit.exceeded` و`platform.*.rate_limit`: تجاوز مؤقت للسرعة، وتظهر له رسالة «أعد المحاولة بعد قليل».
- أي رمز آخر مع 429 يأخذ رسالة SPEC. لا إعادة محاولة تلقائية في أي حالة.

### 2.5 أخطاء الصلاحيات (401/403)
- `platform.<المنصة>.auth` و`platform.<المنصة>.permission_denied` و`account.reconnection_required`: تظهر رسالة SPEC «يلزم إعادة ربط حساب [المنصة] من لوحة SocialAPI».
- `auth.*` (مفتاح SocialAPI نفسه غير صالح) و`auth.insufficient_scope` و`byok.*`: رسائل مستقلة، لأن إعادة ربط الحساب لا تصلحها.

### 2.6 إضافة على مخطط D1: عمود `drafts.notes`
- مخرجات الصياغة فيها حقل `notes`، مثل «تحقق من رقم المادة». بدون عمود يحفظه تضيع الملاحظة عند إعادة عرض المعاينة.
- أُضيف العمود في `migrations/0002_draft_notes.sql`. أما `0001_init.sql` فمنقول من SPEC حرفياً.

### 2.7 الخطة (`plan`)
- نُفّذت في المرحلة 1 لأن زر «اقترح موضوعاً» في تذكير المهمة اليومية (SPEC §9) يحتاجها، ثم أُضيفت مهمة الأحد الأسبوعية في المرحلة 2 (القسم 6).
- أُضيف حقل `pillar` لكل اقتراح، لأن الموضوع الجديد (`idea_id: null`) يُحفظ فكرةً ويحتاج محوراً.
- الموضوع الجديد المحفوظ مصدره `plan`. قائمة المصادر في تعليق SPEC هي text وvoice وphoto فقط.
- الخطة تستخدم `CLAUDE_MODEL`، لأنها مهمة استدلال وليست تصنيفاً خفيفاً.

### 2.8 ترقيم التغريدات للعرض فقط ✅ (اختيار المالك)
- المعاينة تعرض `1/ …` و`2/ …` كما في SPEC §7، لكن الترقيم **لا يُنشر** مع التغريدات، والتعليمات تطلب من Claude ألا يرقّم.
- اختار المالك إبقاء الترقيم للعرض فقط.

### 2.9 استدعاء Anthropic عبر `fetch` بلا SDK
- اتساقاً مع SPEC §2 («استدعاءات fetch مباشرة») وقاعدة «لا اعتماديات كبيرة بدون موافقة». يمكن التحويل إلى `@anthropic-ai/sdk` إن رغبت.
- المخرجات مقيّدة بمخطط JSON عبر `output_config.format` (structured outputs)، ثم يُتحقق منها برمجياً.
- **التفكير الموسّع موقوف** في نموذج الصياغة (`thinking: {type: "disabled"}` على Sonnet 5)، لأن المهلة المتاحة قصيرة (القسم 3).
- تعليمات النظام الخاصة بالكتابة، وفيها `voice.md`، مخزّنة مؤقتاً (`cache_control`)، فتقل التكلفة عند التعديل أو إعادة الصياغة خلال 5 دقائق.
- `voice.md` يُضمَّن في تعليمات الصياغة والتعديل فقط. القواعد المشتركة مضمّنة في كل التعليمات.
- إعادة المحاولة بعد ثانيتين تكون لأخطاء الخدمة القابلة للإعادة فقط: 408 و409 و429 و529 و5xx والشبكة. خطأ مثل 401 أو 400 لا يُعاد لأن الإعادة لا تصلحه.

### 2.10 ما أضيف للحماية من النشر الخاطئ
- **بصمة التأكيد:** زر «تأكيد» يحمل بصمة النسخة والمنصات والصورة. إن تغيّر شيء بعد عرض التأكيد يُرفض الزر القديم، فلا يُنشر محتوى لم تره.
- **انتقال ذرّي للحالة** في D1: ضغطتان على «تأكيد» لا تُنشئان منشورين.
- **التحقق مرتين:** قبل عرض التأكيد، ومرة أخرى قبل الإنشاء مباشرة.
- **رد غير مؤكد:** إذا لم يصل ردّ مؤكد بعد طلب الإنشاء (مهلة أو 5xx)، تُعلَّم المسودة `failed` مع تنبيه بمراجعة لوحة SocialAPI، ولا تعود «معلّقة» تلقائياً تفادياً للتكرار. وإذا أُنشئ المنشور ثم تعثّر ما بعده، يُحفظ معرّفه وتحسم المهمة اليومية حالته.

### 2.11 المهمة اليومية تتابع المجدولات أيضاً
- SPEC §9(أ) يذكر المنشورات العالقة في `publishing`. أضفت المجدولة التي حلّ موعدها، وإلا بقيت حالتها «مجدولة» إلى الأبد ولم يُحدَّث `last_published_at` الذي يعتمد عليه التذكير.
- `last_published_at` لا يُحدَّث إلا بتاريخ أحدث، حتى لا يعيده حسم منشور قديم إلى الوراء.

### 2.12 تفاصيل أخرى
- **الرصيد:** يُقرأ الحد من `posts_limit` في SocialAPI. يُستخدم `MONTHLY_POST_LIMIT` فقط إن غاب الحقل.
- **وضع التجربة:** لا يمنع النشر عند نفاد الرصيد، لأن المسودات مجانية.
- **حالات الانتظار** (`awaiting_edit` و`awaiting_image`):
  - تنتهي بعد ساعة.
  - لكل منها زر «إلغاء».
  - أي أمر يلغيها.
  - النص يلغي انتظار الصورة.
- **الصور:**
  - تُقبل صورة عادية (أكبر نسخة)، أو ملف صورة (document) للحفاظ على الجودة.
  - الصورة بلا تعليق وبلا انتظار لا تُحفظ فكرةً، ويطلب البوت تعليقاً.
- **طول التغريدة:** يُحسب كما يحسبه X (أوزان twitter-text v3): العربية حرف واحد، والرابط 23، والإيموجي حرفان.
- **حد لينكدن:** 3000 حرف، وهو افتراض، ومسار التحقق هو المرجع النهائي.
- **إعادة الطلب عند مخالفة الطول** مرة واحدة كما في SPEC، لكن فقط للمخالفات التي تمنع النشر (تغريدة أطول من الحد، أكثر من 6 تغريدات، لينكدن فوق 3000 حرف). مخالفات عدد كلمات لينكدن وإطارات سناب تظهر تنبيهاً في المعاينة.
- **السجلات:** Workers Logs مفعّلة (ضمن الخطة المجانية). لا تُسجَّل أسرار ولا روابط ملفات تيليجرام ولا نصوص منشورات.
- **اسم الـ Worker:** `social` (مثل اسم المستودع). إن اخترت اسماً آخر في Workers Builds فعدّل `name` في `wrangler.toml`.

---

## 3. المهلة داخل `waitUntil` (SPEC §2)

- `ctx.waitUntil` يمنح نحو 30 ثانية بعد الرد على تيليجرام، ويعمل البوت بميزانية 27 ثانية لكل تحديث.
- مهلة استدعاء Claude تُحسب من الوقت المتبقي، وتُترك ثانيتان ونصف لإبلاغك بالفشل بدل أن تبقى رسالة «جاري الصياغة…» معلّقة.
- إعادة الطلب لتصحيح الطول لا تحدث إلا إن كان الوقت المتبقي يكفي. وإلا تُعرض المسودة مع تنبيه، ويمكنك الضغط على «✏️ عدّل» أو «🔁 صياغة جديدة».
- **إن تكررت رسالة «استغرقت الصياغة وقتاً أطول من المسموح»** فالاقتراح هو الانتقال إلى Cloudflare Queues، ولن أنتقل إليها قبل موافقتك.
- صُممت المعالجة داخل الـ Worker لتبقى خفيفة: لا معالجة صور، والبايتات تُمرَّر كما هي، ومعظم الوقت انتظار شبكة لا يُحتسب. **لم يُقَس وقت المعالج الفعلي** مقابل حد 10ms. راقبه في لوحة Cloudflare (Metrics → CPU Time) خلال أسبوع التجربة.

---

## 4. اختبار الثريد مع لينكدن (معيار القبول 4) — ✅ التحقق والمسودة، ⏳ النشر الفعلي

جُرّب على حساب المالك من البوت نفسه في وضع التجربة (2026-09-23): منشور واحد فيه ثريد X وهدف لينكدن.

- [x] (أ) `platform_data.thread` على هدف X، وهي الطريقة المعتمدة: قبلها التحقق، وأُنشئت المسودة (معرّف SocialAPI `016a5c22-ad36-4fe3-a4a1-d09e88b88aed`). ذلك بعد تحويل عناصر الثريد إلى `{text, media_ids}`؛ قبل التحويل رفضها التحقق (القسم 2.1).
- [ ] (ب) `segments` على مستوى المنشور: لم تُجرَّب، فلا حاجة إليها بعد قبول (أ).
- [ ] المسودة كما خزّنتها SocialAPI: تُراجع من لوحة SocialAPI، لأن `GET /posts/{id}` لا يعيد `platform_data`.
- [ ] النشر الفعلي: أول منشور بعد إيقاف وضع التجربة يؤكد أن الثريد نُشر كاملاً على X، وأن لينكدن نُشر بنصه وحده.

النتيجة بحسب قاعدة القرار أدناه: يبقى منشور واحد برصيد واحد.

للتجربة من الطرفية بدل البوت (اختياري، مجاني):

```sh
SOCIALAPI_KEY='...' node scripts/socialapi-check.mjs thread-test <X_ACCOUNT_ID> <LINKEDIN_ACCOUNT_ID> --draft
```

قاعدة القرار:
- إن قُبلت (أ): يبقى منشور واحد برصيد واحد كما هو الآن.
- إن رُفضت (أ) وقُبلت (ب) بلا مشكلة في لينكدن: التحويل إلى `segments`.
- إن رُفضت الطريقتان: منشوران منفصلان برصيدين، مع تحديث رسالة التأكيد (SPEC §8.3).

---

## 5. الاختبارات

- **وحدات (74):**
  - عدّ الأطوال، وتوقيت الرياض وخيارات الجدولة.
  - تحليل المحاور ومخرجات Claude، وبناء طلب المنشور، ورسائل الأخطاء.
  - تقسيم المعاينة، وحجم بيانات الأزرار (64 بايت)، والتحقق من المالك.
  - قراءة المقاييس، وأداء المحاور، وتعليمات التقرير والخطة، وحساب الرصيد، وترميز base64 للصوت.
  - الإضافات الوظيفية: أزرار كل حالة، وضوابط تغيير الموعد والإلغاء، وتنسيق سكربت سناب، وأزرار الأرشفة.
  - الوصول السريع: مطابقة نصوص الأزرار، وشكل اللوحة، وصلاحية قائمة الأوامر.
  - الإعداد من المتصفح: تقسيم ملفات الترحيل، وتطبيق الناقص منها ذرّياً مع تحمّل البدء المتزامن، واشتقاق سر الـ webhook.
  - اقتراحات الأحداث: بناء الملخص من المصادر، وتعليمات البحث، والخطة بالأحداث ومصادرها.
- **طرفية (106):** تجمّع الـ Worker بـ wrangler وتشغّله داخل workerd (Miniflare) مع D1 محلية، وتعترض طلبات Telegram وAnthropic وSocialAPI بخوادم وهمية.
  - 33 للمرحلة 1: معايير القبول 1 و2 و3 و5 و6 وحالات الفشل.
  - 22 للمرحلتين 2 و3: الأوامر الجديدة، والمهام المجدولة الثلاث، والتقرير في حالاته، واستخدام المقاييس في الخطة، والرسائل الصوتية.
  - 18 للإضافات الوظيفية (القسم 8).
  - 5 للوصول السريع (القسم 9).
  - 19 للإعداد من المتصفح (القسم 10).
  - 9 لاقتراحات الأحداث (القسم 11).
- **فحص الطفرات:** زُرعت أعطال متعمدة في نسخة منفصلة من الكود، فكشفتها الاختبارات كلها.
  - المرحلة 1:
    - نشر في وضع التجربة.
    - حذف التحقق من المالك.
    - تجاوز التحقق.
    - تجاوز البصمة.
    - صورة على X.
    - عدم رفع رقم النسخة.
    - حذف منع التكرار.
    - حذف الانتقال الذرّي.
  - المرحلتان 2 و3:
    - تجاهل نافذة الثلاثين يوماً.
    - احتساب الرقم الغائب صفراً.
    - تجاهل الإيقاف المؤقت.
    - خطة لا تستخدم المقاييس.
    - الخطة الأسبوعية غير مجدولة.
    - استدعاء Claude بلا بيانات.
    - ظهور المنشور في قائمة الانتظار.
  - الصوت:
    - لغة تفريغ خاطئة.
    - بلا حد للمدة.
    - حفظ تفريغ فارغ.
    - إبقاء انتظار التعديل.
  - الإضافات الوظيفية (20 طفرة، كُشفت كلها):
    - إلغاء نافذة الخمس عشرة دقيقة محلياً.
    - حذف أيٍّ من فحوص SocialAPI قبل الحذف: الحالة، والموعد، والأهداف المنشورة، أو الاستغناء عن `GET` كله.
    - إعلان الإلغاء دون التأكد من 404.
    - قبول تأكيد إلغاء قديم.
    - حذف الانتقال الذرّي لإعادة المحاولة.
    - إعادة المحاولة في وضع التجربة، أو بلا رصيد.
    - اعتبار «جزئي» بعد إعادة المحاولة نتيجة نهائية.
    - تطبيق «قيد النشر» عند التحقق من الحالة.
    - أزرار إدارة لمسودة بلا منشور.
    - عرض الموعد الحالي ضمن الخيارات.
    - عدّ المسودات الفاشلة في التذكير.
    - أرشفة فكرة ليست في البنك.
    - إزاحة موضع النص المنسّق في سكربت سناب.
    - تسجيل محتوى النسخة الاحتياطية.
    - إبقاء الأزرار الحيّة في رسالة قديمة بعد الإلغاء.
    - حصر أزرار رسائل النتيجة في المسودات القابلة للتعديل.
  - الوصول السريع (7 طفرات، كُشفت كلها):
    - تجاهل نصوص الأزرار.
    - عدم حذف محدِّد شكل الإيموجي عند المطابقة.
    - زر تذكير لا يتبع الحالة، أو رد إيقاف بلا لوحة محدّثة.
    - قائمة أوامر ظاهرة للجميع.
    - إبقاء انتظار ملاحظات التعديل بعد ضغط زر.
    - فشل ضبط القائمة يوقف الترحيب.
  - الإعداد من المتصفح (30 طفرة، كُشفت كلها):
    - سر مشتق من نص آخر، أو تجاهل السر الصريح، أو ربط تيليجرام بلا سر، أو بإسقاط التحديثات المعلّقة.
    - عرض قيم الإعدادات في `/setup`، أو عرض خطأ تيليجرام بلا تهريب HTML، أو قبول معرّف مالك غير رقمي، أو إسقاط تلميح التوكن الخاطئ، أو إخفاء ما ينقص، أو عرض الوضع معكوساً، أو السماح بتخزينها مؤقتاً، أو ربط العنوان الكامل للطلب، أو قبول POST.
    - تخطي تجهيز الجداول في التحديثات أو في Cron، أو غياب التنبيه حين لا تُربط القاعدة.
    - إعادة ترحيل مطبّق، أو عدم تحمّل البدء المتزامن، أو عدم تسجيل الترحيل، أو ابتلاع فشل حقيقي.
    - عدم التعرف على X، أو عرض صفحات الشركات حساباتٍ للينكدن، أو إزاحة مواضع النسخ، أو إزعاج المالك بفشل الجلب والمعرّفات مضبوطة، أو تجاهل الحساب الذي يحتاج إعادة ربط، أو عدم التنبيه على معرّف لا يطابق، أو الجلب بلا مفتاح، أو فشل الرسالة يحوّل الترحيب إلى خطأ، أو عدم إرسالها.
  - اقتراحات الأحداث (22 طفرة، كُشفت كلها):
    - قبول مصادر من غير نتائج البحث، أو روابط غير http، أو بلا حد، أو إدخال النص الذي يسبق البحث في الملخص.
    - قبول رقم مصدر خارج القائمة، أو اقتراح بلا حدث في نشرة الأربعاء.
    - نشرة الأربعاء تُبطل أزرار الأحد، أو أزرارها لا تعمل.
    - عدم حفظ الأحداث المقترحة، أو عدم انتهاء صلاحيتها، أو عدم إرسالها للبحث التالي.
    - فكرة الحدث بلا سياقه ومصدره، أو بمصدر plan، أو إخفاء سطر المصدر.
    - فشل البحث يوقف خطة الأحد، أو فشل الأربعاء يُبتلع، أو غياب تفسير البحث المعطّل.
    - عدم استئناف البحث الموقوف، أو عدم إعادة الإجابة الموقوفة، أو تعطيل التفكير مع البحث، أو إسقاط توجيه النتائج إلى السعودية، أو عدم جدولة الأربعاء.
- **الترحيلات:** طُبّقت محلياً بـ `wrangler d1 migrations apply --local` بنجاح. ويطبّقها الـ Worker الآن بنفسه (القسم 10).
- **ما لم يُختبر:** الخدمات الحقيقية نفسها، وهي تحتاج حساباتك: رسائل تيليجرام الفعلية، وجودة صياغة Claude بالعربية، وسلوك SocialAPI الفعلي. هذا هو أسبوع `DRY_RUN` في SPEC §14.7.

---

## 6. المرحلة 2

### ما نُفّذ
- **`/queue`:** المجدولة مرتبة بالموعد، والمعلّقة (ومعها التي فشل نشرها) الأحدث أولاً، والتي قيد النشر.
  - لكل مسودة معلّقة زر «عرض #…» يعيد إرسال معاينتها بأزرارها، لأن أزرار المعاينة القديمة قد تكون بعيدة في المحادثة.
- **`/usage`:** من `GET /v1/usage`. يعرض المستخدَم والحد والمتبقي وموعد تجدد الفترة، ويحذّر إن بقي رصيدان أو أقل.
- **`/pause` و`/resume`:** يوقفان تذكير الانقطاع اليومي ويستأنفانه (المفتاح `reminders_paused` في جدول state).
  - الخطة الأسبوعية وتقرير الخميس يستمران، لأن SPEC §9 يربط الإيقاف بالتذكير وحده، والرد يوضح ذلك.
- **الخطة الأسبوعية:** Cron الأحد `0 5 * * 0` (8:00 ص بتوقيت الرياض) يرسل 3 اقتراحات لكل منها زر «صُغها».
  - هي نفس دالة `/plan`، لكن بلا رسالة انتظار.
  - الفشل يصل بصيغة «⚠️ فشلت مهمة الخطة الأسبوعية: …» (SPEC §9).

### تفريغ الرسائل الصوتية ✅ (وافق المالك ونُفّذ)
عُرض الخيار وتكلفته على المالك كما يشترط SPEC §12 فوافق. المصدر: التوثيق الرسمي لـ Cloudflare (مستودع `cloudflare/cloudflare-docs`، الإيداع `5970c5c` بتاريخ 2026-09-22).

**الخيار: Workers AI، النموذج `@cf/openai/whisper-large-v3-turbo`**
- **العربية مدعومة:** Whisper متعدد اللغات ويشمل العربية. تُمرَّر اللغة `language: "ar"`، و`vad_filter: true` لتجاوز الصمت، و`initial_prompt` بأسماء المحاور لتحسين دقة المصطلحات.
- **يعمل من الـ Worker** عبر ربط `[ai] binding = "AI"` في `wrangler.toml`، بلا مفتاح ولا خدمة خارجية جديدة.
- **التكلفة:** 0.0005 دولار للدقيقة الصوتية، أي 46.63 Neuron للدقيقة.
  - الحصة المجانية اليومية 10,000 Neuron، أي نحو 214 دقيقة يومياً.
  - على خطة Workers المجانية لا فوترة إطلاقاً، فما زاد على الحصة يُرفض حتى اليوم التالي، ويخبرك البوت بذلك.
  - مثال: عشر رسائل صوتية يومياً بدقيقة لكل منها ≈ 466 Neuron، أي نحو 5% من الحصة المجانية، بتكلفة 0 دولار.
- **البدائل التي لم تُعتمد:**
  - `@cf/openai/whisper`: أقدم وأقل دقة.
  - `@cf/deepgram/nova-3`: أغلى 10 مرات، ودعم العربية غير موضح.
  - واجهات خارجية: خدمة جديدة ومفتاح إضافي.

**كيف يعمل:**
1. رسالة صوتية (voice) أو ملف صوتي (audio) ← `getFile` ← تنزيل البايتات داخل الـ Worker.
   - الحد الأقصى 5 دقائق و20MB (حد تيليجرام).
   - التحويل إلى base64 بالدالة الأصلية في بيئة Workers (`Uint8Array.prototype.toBase64`)، فلا يثقل على وقت المعالج.
2. `env.AI.run(...)` ← يُحفظ النص فكرةً مصدرها `voice`، ثم يُصنّف كالنص العادي.
3. يعرض الرد النص المفرّغ لتتحقق منه، مع زر «صُغها الآن».
4. حالات أخرى:
   - إن لم يُتعرَّف على كلام، لا يُحفظ شيء.
   - عند الفشل يظهر زر «أعد المحاولة» على الرسالة الصوتية نفسها (مرجعها محفوظ في state).
   - نفاد الحصة اليومية له رسالة خاصة.
   - الرسالة الصوتية تلغي أي انتظار لملاحظات تعديل، فلا تُفسَّر ملاحظاتٍ.
- **التشغيل المحلي:** ربط AI يعمل عن بُعد دائماً، فيطلب `npm run dev` تسجيل الدخول إلى Cloudflare. في الاختبارات يحاكيه عامل وهمي عبر RPC.

---

## 7. المرحلة 3: التحليلات

### ما نُفّذ
- **المقاييس:** من `GET /v1/posts/{pid}/metrics` لكل منشور نُشر عبر البوت خلال آخر 30 يوماً، بأربعة طلبات متوازية كحد أقصى.
  - الأرقام الأربعة: الإعجابات والتعليقات والمشاركات والحفظ.
  - الحقول بتسمية OpenAPI (`likes`…)، مع قبول التسمية البديلة الموثقة في أمثلة التوثيق (`like_count`…).
- **الرقم الغائب «غير متوفر» وليس صفراً**، تطبيقاً لقاعدة «لا يُستنتج ما لا تدعمه الأرقام». لينكدن الشخصي قد لا يُرجع أرقاماً أصلاً، لأن توثيق SocialAPI يذكر أن قراءة منشورات العضو من لينكدن تتطلب صلاحيات موسّعة غير متاحة.
- **التفاعل** = مجموع الأرقام المتاحة. هو أساس «أعلى منشور» ومتوسط كل محور.
- **`/stats` وتقرير الخميس** (Cron `0 14 * * 4`، أي 5:00 م بتوقيت الرياض): المخرج بالحقول الأربعة المطلوبة (`headline` و`top_post` و`insight` و`next_week_tip`) في رسالة قصيرة.
  - لا يُستدعى Claude إطلاقاً إن لم يُنشر شيء، أو إن لم تُرجع المنصات أي رقم. يُقال ذلك صراحة بدل تقرير مُتخيَّل.
  - إن قلّ عدد المنشورات المقيسة عن 3، يُطلب من Claude التصريح بقلة البيانات، ويُضاف سطر ثابت «البيانات قليلة، فالاستنتاجات مبدئية».
  - إن فشل جلب المقاييس لكل المنشورات يظهر الخطأ نفسه (مثل مفتاح غير صالح)، ويُعرض لـ `/stats` زر «أعد المحاولة». فشل منشور واحد لا يُسقط التقرير، ويُذكر أمامه «تعذّر جلب المقاييس».
  - التقرير يستخدم `CLAUDE_MODEL`، لأنه تحليل وليس مهمة خفيفة.
- **استخدام المقاييس في الخطة:** كل تقرير يحفظ لقطة مقاييس (`metrics_snapshot` في جدول state). تمرر الخطة منها أداء المحاور وأعلى منشور، مع توجيه بالترجيح دون إهمال التوازن. وتنبّه إن كانت الأرقام قليلة.
  - `/plan` يستخدم اللقطة كما هي، حتى لا يستدعي المنصات داخل مهلة الـ 27 ثانية.
  - خطة الأحد تحدّث اللقطة أولاً إن كانت أقدم من 7 أيام. فشل التحديث لا يوقف الخطة.
- **تكلفة القراءة على X:** كل طلب مقاييس يحدّث الأرقام من المنصة. على X (BYOK) قد تُحتسب القراءة ضمن استهلاك تطبيقك في منصة المطورين. المتوقع هنا طلبات قليلة جداً: منشورات الشهر فقط، مرة أسبوعياً، أو عند `/stats`.

---

## 8. الإضافات الوظيفية

### ما نُفّذ
- **إدارة المنشورات المجدولة:** زر «عرض» في `/queue` يعيد معاينة المسودة بأزرار حالتها. للمجدولة: «🕒 غيّر الموعد» و«🚫 ألغِ الجدولة»، وتظهر الأزرار نفسها في رسالة «جُدولت».
- **إعادة محاولة المنشور الجزئي:** إن نُشر على منصة وفشلت الأخرى يظهر زر «🔁 أعد محاولة ما فشل».
- **«🔄 تحقق من الحالة»** لمسودة قيد النشر: يجلب حالتها من SocialAPI فوراً بدل انتظار المتابعة اليومية.
- **تذكير أذكى:** إن وُجدت مسودات معلّقة يبدأ التذكير بها («لديك مسودتان جاهزتان للنشر») مع زر «عرض» لأحدث ثلاث، ثم زرّا الموضوع الجديد والأفكار.
- **أرشفة الأفكار من `/ideas`:** زر «🗄 أرشف» لكل فكرة (تغيير حالة، لا حذف)، ويتحوّل في الرسالة نفسها إلى «↩️ تراجع».
- **«📋 سكربت سناب»:** يرسل السكربت في رسالة مستقلة، كل إطار كتلة نص منسّقة (`pre`). الزر متاح في كل الحالات عدا المتجاهلة، ومنها رسالة «نُشرت».
- **`/export`:** ملف JSON فيه كل الأفكار والمسودات يصلك في المحادثة.

### إلغاء الجدولة ✅ (وافق المالك بالضوابط)
يستخدم `DELETE /posts/{pid}`، وهو المسار الموثّق للإلغاء. لكن `openapi.json` يقول إن المسار نفسه يحذف المنشور المنشور فعلاً من المنصات، فوُضعت هذه الضوابط:
1. **في البوت:** المسودة مجدولة ولها منشور في SocialAPI، وموعدها بعد أكثر من 15 دقيقة.
2. **ضغطة «تأكيد»** تحمل بصمة الحالة والمنشور والموعد. إن تغيّر شيء بعد عرض التأكيد يُرفض الزر القديم.
3. **في SocialAPI مباشرة قبل الحذف (`GET`):** الحالة `scheduled`، والموعد بعد أكثر من 15 دقيقة، ولا يوجد هدف `published` أو `publishing`. إن لم يتحقق شرط لا يُحذف شيء، وتُطبَّق الحالة الحقيقية محلياً.
4. **التأكد بعد الحذف:** لا يُعلن الإلغاء إلا إذا رد `GET` بـ 404، وهذا سلوك موثّق للمنشور المحذوف. غير ذلك يُبلَّغ بصراحة ولا تتغيّر الحالة المحلية.
5. بعد الإلغاء تعود المسودة «معلّقة» بلا منشور، ويمكن نشرها أو جدولتها من جديد.

- **غير معروف:** هل يعيد SocialAPI رصيد المنشور الملغى؟ التوثيق لا يذكر ذلك. راجع `/usage` بعد أول إلغاء.

### تغيير الموعد
- عبر `PATCH /posts/{pid}` بالحقل `scheduled_at` فقط، وهو مجاني.
- يعمل بنافذة الخمس عشرة دقيقة نفسها، ويختار الموعد الجديد من خيارات الجدولة المعتادة دون الموعد الحالي.
- إن بدأ النشر يرد SocialAPI بـ 409، فيعرض البوت ذلك ويطبّق الحالة الحقيقية.

### إعادة المحاولة
- **للجزئي فقط:** المسودة التي فشل نشرها كلها قابلة للتعديل، وقد يتغيّر نصها بعد الفشل، و`retry` يعيد نشر النص القديم المحفوظ في SocialAPI. لذلك تبقى إعادة نشرها بزر «انشر الآن» من النص الحالي.
- **الرصيد:** تستهلك رصيداً واحداً، فيمر الطلب بحارس الرصيد (يمنع عند 0)، وبضغطة «تأكيد»، وبانتقال ذرّي يمنع إعادتين من ضغطتين.
- **وضع التجربة:** لا تعمل فيه، لأن إعادة المحاولة نشر فعلي.
- **الجزء الفاشل وحده:** يُفهم من رمز الخطأ الموثّق `post.no_retryable_deliveries` («لا توصيلات فاشلة لإعادة محاولتها»). يُتأكد منه في أول تجربة حقيقية.
- **النتيجة:** لا يُعلن فوراً إلا «نُشرت» كاملة. بقاء «جزئي» بعد الطلب مباشرة قد يعني أن المحاولة لم تبدأ بعد، فتبقى المسودة «قيد النشر» مع زر «تحقق من الحالة»، وتحسمها المتابعة اليومية إن لم تضغطه.

### تفاصيل أخرى
- **وضع التجربة لا يُنشئ منشورات مجدولة أو جزئية**، فلا يمكن تجربة تغيير الموعد والإلغاء وإعادة المحاولة فيه. جرّبها بعد إيقافه على منشور مجدول لليوم التالي.
- **الأزرار الحيّة للمسودة** تنتقل إلى آخر رسالة نتيجة، وتُزال من الرسالة السابقة.
- **التذكير** يعدّ المعلّقة فقط، لا التي فشل نشرها.
- **الأرشفة** تقرأ أزرار رسالة `/ideas` كما تصل مع الضغطة. إن لم تصل (رسالة قديمة جداً) تتم الأرشفة ويظهر إشعار فقط.
- **النسخ بلمسة واحدة** سلوك تطبيقات تيليجرام مع النص المنسّق، وليس جزءاً من توثيق Bot API. تأكد منه على هاتفك.
- **`/export`:**
  - يشمل الأفكار والمسودات فقط، دون جدول state.
  - لا يُسجَّل منه في السجلات إلا الأعداد.
  - لا يوجد استرجاع تلقائي من الملف. عند الحاجة يُستورد يدوياً إلى D1.

---

## 9. الوصول السريع ✅ (اختيار المالك: لوحة الأزرار وقائمة الأوامر معاً)

- **لوحة أزرار ثابتة** أسفل المحادثة (`ReplyKeyboardMarkup` مع `is_persistent`)، بثمانية أزرار: أفكاري، قائمة الانتظار، اقترح موضوعات، تقرير الأداء، الرصيد، نسخة احتياطية، التذكير، مساعدة.
- **ضغطة الزر تصل رسالةً نصها نص الزر.** لذلك تُطابق بالنص الكامل بعد حذف محدِّد شكل الإيموجي (U+FE0F) والمسافات الطرفية، ثم تُعامل أمراً:
  - لا تُحفظ فكرةً، ولا يُستدعى Claude.
  - تُلغي انتظار ملاحظات التعديل أو الصورة، كما تفعل الأوامر المكتوبة.
- **نص يطابق زراً حرفياً** (مثل «💡 أفكاري») يُعامل أمراً لا فكرة. الاحتمال ضعيف لأن نص الزر يشمل الإيموجي.
- **زر التذكير يتبدّل** بين «⏸ أوقف التذكير» و«▶️ استأنف التذكير»:
  - رد الإيقاف والاستئناف يحمل اللوحة محدّثة.
  - `/start` و`/help` يعرضانها بحسب الحالة الحالية.
- **متى تُرسل اللوحة:** مع `/start` و`/help` وردّي الإيقاف والاستئناف فقط، لأن الرسالة الواحدة لا تحمل أزرار رسالة ولوحة ثابتة معاً. بعد إرسالها تبقى ظاهرة.
- **قائمة الأوامر** في زر «القائمة»:
  - تُضبط بـ `setMyCommands` بنطاق محادثة المالك وحده، فلا تظهر لغيره.
  - تُضبط عند كل `/start`، وفشلها لا يوقف الترحيب.
- **بعد النشر** أرسل `/start` مرة لتظهر اللوحة والقائمة.

---

## 10. الإعداد من المتصفح ✅ (طلب المالك: بلا طرفية، وأقل قدر من الأسرار والتفاصيل)

### ما تغيّر
- **الـ Worker ينشئ جداوله بنفسه.**
  - ملفات `migrations/` مضمّنة في الكود كما هي (`src/migrations.ts`)، وتُطبَّق عند أول طلب لكل نسخة من الـ Worker: تحديث من تيليجرام، أو مهمة Cron، أو فتح `/setup`.
  - تُسجَّل في جدول `d1_migrations` بصيغة Wrangler نفسها، فما طبّقه `wrangler d1 migrations apply` لا يُعاد، والعكس.
  - كل ترحيل يُنفَّذ مع تسجيله في دفعة واحدة ذرّية: إما الاثنان أو لا شيء. وإن سبق طلبٌ متزامن إلى الترحيل نفسه، يُكتفى بما سجّله.
  - يُحفظ النجاح لكل نسخة لا الوعد الجاري، لأن انتظار وعد بدأه طلب آخر قد يعلق إن انتهى ذلك الطلب قبله.
  - النتيجة: لا خطوة ترحيل في البناء، ولا رمز API بصلاحية D1.
- **ربط D1 بلا معرّف في `wrangler.toml`.**
  - عند أول نشر ينشئ Wrangler القاعدة `social-db` ويربطها باسم `DB` (Automatic provisioning، تجريبي منذ Wrangler 4.45). وتبقى مربوطة في النشرات التالية دون معرّف.
  - **غير مؤكد:** رمز البناء الذي تنشئه Workers Builds تلقائياً لا يذكر التوثيق فيه صلاحية D1، بينما يقول توثيق الإعداد إن النشر من GitHub ينشئ الموارد. إن رُفض الإنشاء: تُنشأ القاعدة وتُربط باسم `DB` من اللوحة ثم يُعاد البناء. عندها يرث النشر الربط القائم دون أي استدعاء لـ D1 API (رأيته في كود Wrangler 4.136.3). إن لم يظهر الـ Worker بعد فشل أول بناء، فالبديل: إنشاؤه من «Start with Hello World» باسم `social`، وربط القاعدة والأسرار، ثم Settings ← Builds ← Connect.
- **القيم الشخصية في لوحة Cloudflare لا في المستودع.**
  - معرّف المالك ومعرّفا الحسابين و`DRY_RUN` صارت متغيرات نصية تُضبط من اللوحة، و`keep_vars = true` يمنع النشر من مسحها.
  - القيم العامة (النماذج والحدود) بقيت في `[vars]`.
  - غياب `DRY_RUN` يعني وضع التجربة، وهو الافتراض الآمن كما كان.
- **سر الـ webhook مشتق من التوكن** ⚠️ انحراف عن SPEC §3 للموافقة: `TELEGRAM_WEBHOOK_SECRET` صار اختيارياً.
  - بدونه يُحسب: HMAC-SHA256 بمفتاح هو توكن البوت، على النص `social-bot:telegram-webhook-secret`، بترميز base64url. الناتج 43 حرفاً من الأحرف التي يقبلها تيليجرام.
  - الحجة: من يملك التوكن يملك البوت أصلاً، ويستطيع تغيير الـ webhook نفسه، فالاشتقاق لا يضعف الحماية. ويبقى التحقق كما في SPEC §4.1: مقارنة ثابتة الزمن، و401 عند عدم التطابق.
  - إن ضُبط السر صراحةً فهو المعتمد. وتغيير التوكن يغيّر السر المشتق، فتُفتح `/setup` بعده لإعادة الربط.
- **صفحة `/setup`:**
  - GET فقط، ولا تقبل أي مدخلات.
  - لا تعرض أي قيمة: أسماء الإعدادات الناقصة فقط، مع نوع كل منها (Secret أو Text).
  - تربط تيليجرام بعنوان الطلب نفسه (`/webhook`)، مع `allowed_updates` لـ `message` و`callback_query`، ودون إسقاط التحديثات المعلّقة. تكرارها لا يضر.
  - تُهرّب رسائل الخطأ الآتية من تيليجرام، وتمنع التخزين المؤقت، وتطلب من محركات البحث عدم فهرستها.
  - من يفتحها غير المالك لا يستطيع إلا إعادة الربط نفسه بالعنوان نفسه، ومعرفة أسماء ما ينقص.
- **لا روابط للنسخ القديمة ولا للمعاينات** (`preview_urls = false`).
  - بدونه يحصل كل نشر على رابط عام لنسخته (Version URL)، يعمل بأسرار الإنتاج وقاعدته. ولو فتح أحدٌ `/setup` من رابط نسخة قديمة، لرُبط تيليجرام بها.
  - معاينات الفروع غير `main` (Worker Previews) لا ترث إعدادات الإنتاج أصلاً. قسم `[previews]` الفارغ يجعلها تُبنى بلا قاعدة بيانات ولا أسرار ولا روابط؛ وبدونه يفشل بناؤها في Workers Builds.
- **معرّفات الحسابات من `/start`** بدل سكربت الطرفية.
  - إن نقص معرّف أو لم يطابق حساباً مربوطاً، يعرض البوت للمالك وحده حساباته من `GET /accounts` (صلاحية `accounts:read`)، مع المعرّفات بتنسيق يُنسخ بلمسة.
  - يُعرض لـ X ما منصته `twitter`، وللينكدن ما منصته `linkedin` فقط؛ صفحات الشركات (`linkedin_page`) ليست ضمن البوت.
  - ينبّه إن احتاج حساب مضبوط إلى إعادة ربط.
  - إن فشل الجلب والمعرّفات مضبوطة فلا رسالة، حتى لا يُزعج المالك إن كان المفتاح بلا `accounts:read` عمداً.
- **إن لم تُربط القاعدة:** تقول `/setup` كيف تُربط، ويرد البوت على أي رسالة بتنبيه يوجّه إليها بدل «خطأ غير متوقع».
- **`scripts/set-webhook.sh`** باقٍ لمن يستخدم الطرفية، ويحتاج `TELEGRAM_WEBHOOK_SECRET` مضبوطاً صراحةً بالقيمة نفسها في Cloudflare.

### للموافقة
- جعل `TELEGRAM_WEBHOOK_SECRET` اختيارياً والاشتقاق من التوكن عند غيابه (انحراف عن SPEC §3). إن فضّلت سراً مستقلاً، أضفه بنوع Secret ثم افتح `/setup`.

---

## 11. اقتراحات الأحداث ✅ (موافقة المالك 2026-09-23: الأحد والأربعاء، وأخبار المحاور مع الرائج العام)

### ما نُفّذ
- **خطة الأحد 8 ص:** يبحث Claude أولاً عن أحداث آخر 7 أيام، ثم تبني الخطة عليها اقتراحاً أو اثنين، والباقي من بنك الأفكار كما كان. تحت الاقتراح المبني على حدث سطر «المصدر» برابطه.
- **نشرة الأربعاء 8 ص:** مهمة Cron رابعة (`0 5 * * 3`). تبحث في آخر 4 أيام، وتقترح حتى 3 موضوعات من الأحداث وحدها بأزرار «صُغها». إن لم تجد ما يستحق تقول ذلك في سطر واحد.
- **ما يُبحث عنه:** أخبار المحاور (أنظمة ولوائح وقرارات وتقارير ومبادرات وفعاليات في السعودية والخليج)، والرائج العام في السعودية إن أمكن ربطه بأحد المحاور بزاوية مهنية.
- **«صُغها» على اقتراح حدث:** تُحفظ فكرة مصدرها `event`، ونصها الزاوية والحدث والمصدر. هكذا تستند الصياغة إلى الحدث المنقول، لا إلى ذاكرة النموذج، ولا تخالف قاعدة منع الاختلاق (SPEC §10) التي تسمح بما في نص الفكرة فقط.
- **لا تكرار:** تُحفظ الأحداث المقترحة أسبوعين (`recent_events`)، ويُطلب من البحث التالي تجنّبها.
- **أزرار الأحد تبقى صالحة بعد الأربعاء:** لكلٍّ منهما مفتاح حفظ منفصل (`last_plan` و`last_events_plan`).
- **`/plan` عند الطلب لا يبحث:** مهلة الرد على تيليجرام 27 ثانية (القسم 3)، والبحث قد يستغرق دقائق. البحث في المهام المجدولة وحدها، بمهلة 6 دقائق من 15 تسمح بها Cloudflare للمهام المجدولة.

### كيف يعمل البحث
- **أداة `web_search` من Anthropic** بمفتاح `ANTHROPIC_API_KEY` نفسه، بلا خدمة ولا سر جديد. حد أقصى 5 عمليات بحث في المرة، ونتائج موجّهة إلى السعودية (`user_location`: `SA` و`Asia/Riyadh`). النموذج هو `CLAUDE_MODEL`.
- **النسخة الأساسية `web_search_20250305`**، لا نسخ التصفية الديناميكية الأحدث. تلك تشغّل البحث من داخل تنفيذ كود، ومتابعتها بعد الإيقاف المؤقت أعقد، ولم يمكن تجربتها هنا. الأساسية أبسط وموثقة، وتستهلك توكنات أكثر قليلاً.
- **خطوتان لا خطوة واحدة:** توثيق Anthropic يذكر أن المخرجات المقيّدة بمخطط JSON لا تجتمع مع citations، ونتائج البحث تحملها دائماً. لذلك يعطي البحث ملخصاً نصياً مرقّم المصادر، ثم يختار منه استدعاء الخطة المعتاد (بمخطط JSON) ويشير إلى رقم المصدر.
- **الروابط من نتائج البحث وحدها:** تؤخذ من citations من نوع `web_search_result_location`، وأي رقم مصدر لا يطابقها يُهمل. فلا يظهر رابط اخترعه النموذج.
- **التفكير المتكيّف بجهد متوسط** في استدعاء البحث وحده: تعطيل التفكير مع الأدوات قد يجعل النموذج يكتب طلب البحث نصاً بدل استدعائها. بقية الاستدعاءات كما كانت.
- **الإيقاف المؤقت (`pause_turn`):** تُعاد الإجابة كما هي فيكمل البحث من حيث توقف، حتى 3 مرات.
- **الفشل:** في خطة الأحد لا يوقف الخطة، ويُذكر سببه في آخرها. في الأربعاء يُبلَّغ فشلاً للمهمة كبقية المهام. وإن كان البحث في الويب معطّلاً من Console تقول الرسالة ذلك.

### التكلفة
- 10 دولارات لكل ألف عملية بحث، إضافة إلى توكنات النتائج. تقديراً: 0.15 إلى 0.25 دولار في المرة، أي نحو 1.5 إلى 2 دولار شهرياً للأحد والأربعاء معاً.
- يظهر عدد عمليات البحث في سجلات الـ Worker (الحقل `searches` في سطر `claude`).

### حدود
- الوسوم الرائجة لحظياً في X غير متاحة بدقة عبر البحث، وتحتاج اشتراكاً مدفوعاً في X API؛ لم تُضف.
- جودة الاقتراحات تعتمد على ملف المحاور. قبل تعبئته يعتمد البحث على وصف المالك العام (محامٍ ومستشار حوكمة وتخطيط استراتيجي).
- لم يُجرَّب البحث على حساب Anthropic الحقيقي بعد، لأن الشبكة محجوبة في بيئة البناء. أول تشغيل فعلي في أول أحد أو أربعاء بعد النشر.

### انحراف عن SPEC
- SPEC §9 يحدد ثلاث مهام مجدولة؛ صارت أربعاً بموافقة المالك، ضمن حد الخطة المجانية (5 للحساب كله).
- خطة الأحد صارت تستعمل بحث الويب، وهو خدمة مدفوعة من Anthropic نفسها؛ وافق عليها المالك (SPEC §13).

